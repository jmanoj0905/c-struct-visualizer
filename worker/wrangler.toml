name = "c-trace-worker"
main = "src/index.ts"
compatibility_date = "2024-06-01"

# Secrets (set via `wrangler secret put`):
# BACKEND_URL   - e.g. http://your-vps-ip:3001
# API_SECRET    - shared secret with the backend
# ALLOWED_ORIGIN - e.g. https://your-app.pages.dev
